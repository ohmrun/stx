package stx.parse.lift;

class LiftParseFailureError{
  // static public function toParseFailure_with<P,R>(self:Error<ParseFailure>,rest:ParseInput<P>,fatal=false):Error<ParseFailure>{
  //   return self.errate(
  //     e -> ParseFailure.make(rest.position(),e,fatal)
  //   );
  // }
}